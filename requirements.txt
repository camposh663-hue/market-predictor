# HTTP client used by concrete market-data providers.
requests>=2.31.0,<3.0.0

# Tabular data handling and Parquet I/O used by ParquetRepository.
pandas>=2.0.0,<3.0.0
pyarrow>=14.0.0,<19.0.0

# Log-return calculations used directly by TechnicalIndicatorCalculator.
numpy>=1.26.0,<3.0.0

# Technical indicator calculations used by TechnicalIndicatorCalculator.
# pandas-ta-classic is a maintained fork of pandas-ta; the original package
# imports `numpy.NaN`, removed in numpy>=2.0, and is unavailable for this
# Python version.
pandas-ta-classic>=0.6.0

# Baseline model and evaluation metrics used by RandomForestTrainer.
# joblib is a scikit-learn dependency; listed explicitly because
# scripts/train_model.py imports it directly to persist trained models.
scikit-learn>=1.3.0,<2.0.0
joblib>=1.3.0

# Gradient-boosted trees used by XGBoostTrainer, the second interchangeable
# ModelTrainer implementation compared against RandomForestTrainer.
xgboost>=2.0.0,<3.0.0
